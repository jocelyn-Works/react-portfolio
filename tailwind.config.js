/** @type {import('tailwindcss').Config} */
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};

// chat gpt Aceternity UI => react
// convert it to js and remove all the the type script annotations and make it suitable to use for vit and react
